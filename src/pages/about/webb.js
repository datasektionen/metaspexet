import React from 'react';
import Layout from '../../components/Layout';

const Webb = () => {
    return (
        <Layout>
            <h1>Hello from web group</h1>
        </Layout>
    );
};

export default Webb;
